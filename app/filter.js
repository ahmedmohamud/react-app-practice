
    // JavaScript to illustrate findIndex() method
    function ahmedAge(age) {
        return age >= 15;
    }
  
    function func() {
        var filtered = [24, 33, 16, 40].filter(ahmedAge);
        console.log(filtered);
    }
    func();

    
// JavaScript to illustrate findIndex() method
function studentsAge(age) {
    return age >= 15;
}

function func() {
    var filtered = [24, 33, 16, 40].filter(studentsAge);
    console.log(filtered);
}
func();

let mydinner = [
    { name: "burger", price: 500},
    { name: "meat", price: 600},
    { name: "chicken", price: 400},
]
let totalPrices = [500, 600, 400];
{
    console.log(500+600+400);
}



            